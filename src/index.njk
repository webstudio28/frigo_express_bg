---
layout: base.njk
title: Начало
description: ВЕГА ТЕСТ - компютърна диагностика за здравословно хранене с най-подходящите храни за вашия организъм
---


<!-- Hero Section Only -->
<section class="relative h-screen flex items-center justify-center overflow-hidden text-white text-center px-4 text-shadow">
    <div class="absolute inset-0 pointer-events-none z-0" style="background: linear-gradient(to bottom, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0) 150%);"></div>
    
    <!-- Background decorative images -->
    <img src="{{ '/assets/images/europe-map.png' | url }}" alt="Карта на Европа" class="absolute top-1/2 -translate-y-1/2 h-[95vh] w-auto object-contain pointer-events-none z-[1]" style=" left: -40%; opacity: 0.2; rotate: ;">
    <img src="{{ '/assets/images/thermometer.png' | url }}" alt="Термометър" class="absolute top-1/2 -translate-y-1/2 h-[55vh] w-auto object-contain pointer-events-none [1]" style="right: 0; right: -5%; opacity: 0.5;">
    
    <div id="hero-cars" class="absolute bottom-0 left-0 right-0 h-[40%] w-full overflow-hidden pointer-events-none z-[2]">
        <img src="{{ '/assets/images/hero-cars.png' | url }}" alt="Хладилен автопарк на Frigo Express BG" class="w-full h-full object-fill">
    </div>

    <div class="relative z-10 max-w-4xl w-full  flex flex-col items-center mb-[20vh] gap-6">
        <div class="text-3xl md:text-4xl lg:text-6xl font-semibold leading-tight space-y-4">
            <div class="flex flex-col items-center gap-2 tracking-wide">
                <div class="flex items-center justify-center gap-3">
                    <span>Нашият бизнес</span>
                    <img src="{{ '/assets/images/bus-icon-dark.png' | url }}" alt="Иконка хладилен бус" class="h-8 w-auto md:h-10 vertical-align-middle pt-2">
                </div>
                <div class="flex items-center justify-center gap-3">
                    <span>движи</span>
                    <span class="text-[#C8E6C9] text-4xl md:text-5xl lg:text-6xl font-semibold">вашият</span>
                    <span>бизнес</span>
                </div>
            </div>

        <p class="text-white/80 font-light md:text-lg lg:text-xl max-w-2xl">
            Хладилен транспорт за храни, лекарства и мостри по европейските маршрути
        </p>

        <a href="{{ '/kontakti/' | url }}" class="inline-flex items-center justify-center rounded-full bg-white text-[#25425E] px-6 py-2 text-sm md:text-base font-semibold shadow-md hover:shadow-lg transition-all duration-200">
            Обади ни се
        </a>
    </div>
</section>

<!-- Why choose us Section -->
<section class="py-12 flex">
    <div class="max-w-7xl w-full mx-auto px-4 flex flex-col justify-center items-center gap-12 md:gap-14">
        <!-- Header -->
        <div class="w-full text-left z-[100]">

            <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary-blue mb-4">
                <div class="flex items-center gap-3">
                    <span>Защо да изберете Frigo Express BG</span>
                    <img src="{{ '/assets/images/trust.png' | url }}" alt="Trust icon" class="h-8 w-auto md:h-8 lg:h-10">
                </div>
            </h2>
            <p class="text-medium md:text-lg text-primary-deepsea leading-relaxed">
                Работим из цяла Европа и осигуряваме надежден хладилен транспорт за храни, лекарства и мостри. Постоянна температура, прецизна логистика и пълна сигурност за вашата пратка — от началната точка до България и обратно.
            </p>
        </div>

        <div class="approach-container grid lg:grid-cols-2 items-stretch gap-12">

            <!-- Left Side: Image -->
            <div id="approach-media" class="order-1 sm:order-1 relative">
                <div class="rounded-2xl overflow-hidden shadow-xl relative h-full">
                    <img src="{{ '/assets/images/why-choose.jpg' | url }}" alt="При мен в БиоВел" class=" border-primary-dune rounded-2xl w-full h-full object-cover">
                    
                    <!-- Statistics Cards -->
                    <div class="absolute left-0 top-0 bottom-0 md:bottom-0 md:top-auto md:left-0 md:right-0 flex flex-col md:flex-row gap-1 md:gap-4 p-1.5 md:p-4 h-full md:h-auto">
                        {% for stat in statistics %}
                        <div class="bg-white/70 backdrop-blur-sm rounded-lg md:rounded-xl shadow-lg p-1.5 md:p-2 flex-1 flex flex-col items-center justify-center text-center max-w-[150px]">
                            <div class="text-sm md:text-lg lg:text-xl font-bold text-primary-blue mb-0 md:mb-1 leading-tight">{{ stat.number }}</div>
                            <div class="text-[8px] md:text-[10px] lg:text-xs text-primary-dark-gray leading-tight">{{ stat.description }}</div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Right Side: Text Content and Features -->
            <div class="order-2 sm:order-2 flex flex-col justify-between gap-6">
                <!-- Text Content -->
                <div class="flex flex-col gap-4 md:gap-6">
                    <p class="text-medium md:text-md text-primary-deepsea leading-relaxed">
                        Ако търсите сигурността на контролирана температура, точност до минута и гарантирано качество при доставката, вие сте на правилното място.
                    </p>
                    <p class="text-medium md:text-md text-primary-deepsea leading-relaxed">
                        Ние предлагаме премиум хладилен транспорт от България до всяка точка в Европа и обратно – с модерни, надеждни и сертифицирани хладилни бусове, които поддържат идеалните условия за вашите продукти през целия път.
                    </p>
                </div>
                
                <!-- CTA Button -->
                <div class="w-full mt-2">
                    <a href="{{ '/za-nas/' | url }}" class="w-full flex items-center justify-center rounded-full bg-accent-green text-primary-blue px-8 py-3 text-base  font-semibold shadow-md hover:shadow-lg transition-all duration-200">
                        Вижте повече за нас
                    </a>
                </div>
            </div>
        </div>

    </div>
</section>



<!-- Services Section -->
<section class="py-12  flex">
    <div class="max-w-7xl w-full mx-auto px-4 flex flex-col justify-center items-center gap-12 md:gap-14">
        <!-- Header -->
        <div class="w-full text-left z-[100]">
            <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary-blue mb-4">
                <div class="flex items-center gap-3">
                    <span>Нашите услуги</span>
                    <img src="{{ '/assets/images/gps.png' | url }}" alt="GPS icon" class="h-8 w-auto md:h-8 lg:h-10">
                </div>
            </h2>
            <p class="text-medium md:text-lg text-primary-deepsea leading-relaxed">
                Предлагаме надежден хладилен транспорт за български фирми в цяла Европа — с постоянна температура, бързи маршрути и максимална сигурност за вашите стоки.
            </p>
        </div>

        <!-- Services Carousel -->
        {% set homeServices = services | default([]) %}
        {% if (homeServices | length) == 0 and collections and collections.services %}
            {% set homeServices = collections.services %}
        {% endif %}

        <div class="w-full" data-services-carousel>
            <div class="relative flex items-center gap-2">
                <!-- Left Arrow -->
                <button
                    type="button"
                    class="services-carousel-prev flex-shrink-0 inline-flex items-center justify-center rounded-full bg-white text-primary-blue shadow-md hover:shadow-lg border border-primary-blue/10 w-7 h-7 md:w-10 md:h-10 transition-all duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary-blue/60"
                    aria-label="Предишна услуга"
                >
                    <svg class="w-3.5 h-3.5 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                </button>

                <!-- Cards Container -->
                <div class="flex-1 overflow-hidden px-2 py-2">
                    <div class="services-carousel-track flex gap-6 transition-transform duration-300 ease-in-out" data-services-track>
                        {% for service in homeServices %}
                            <div class="services-carousel-card border  flex-shrink-0 flex flex-col gap-5 bg-white rounded-3xl shadow-md p-2 md:p-6" data-service-card>
                                <div
                                    class="h-40 w-full rounded-2xl flex items-center justify-center"
                                    style="background-color: {{ service.serviceCardColor }};"
                                >
                                    {% if service.serviceIcon %}
                                        <img
                                            src="{{ service.serviceIcon | url }}"
                                            alt="{{ service.name }}"
                                            class="h-16 w-auto object-contain"
                                        >
                                    {% endif %}
                                </div>
                                <div class="flex flex-col gap-3 flex-1">
                                    <h3 class="text-lg font-semibold text-primary-blue leading-snug">
                                        {{ service.name }}
                                    </h3>
                                    <p class="text-sm text-primary-deepsea leading-relaxed">
                                        {{ service.description }}
                                    </p>
                                </div>
                                <div class="w-full flex justify-end">
                                    <a
                                        href="{{ ('/uslugi/' ~ service.slug ~ '/') | url }}"
                                        class="inline-flex items-center gap-2 rounded-full bg-primary-blue text-white px-5 py-2 text-sm font-semibold shadow-md hover:shadow-lg transition-all duration-200"
                                    >
                                        Вижте услугата
                                    </a>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>

                <!-- Right Arrow -->
                <button
                    type="button"
                    class="services-carousel-next flex-shrink-0 inline-flex items-center justify-center rounded-full bg-white text-primary-blue shadow-md hover:shadow-lg border border-primary-blue/10 w-7 h-7 md:w-10 md:h-10 transition-all duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary-blue/60"
                    aria-label="Следваща услуга"
                >
                    <svg class="w-3.5 h-3.5 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
</section>

                <!-- Транспорт Section -->

                <section class="py-12">
                    <div class="max-w-7xl w-full mx-auto px-4 flex flex-col justify-center items-center gap-12 md:gap-14">
                        <div class="w-full text-left">
                            <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-[#25425E] mb-4">
                                Температурно-контролирани превозни средства
                            </h2>
                            <p class="w-full text-medium md:text-lg text-primary-deepsea max-w-5xl leading-relaxed">
                                Автопаркът на Frigo Express BG покрива целия спектър на хладилния транспорт – от компактни градски ванове до тежкотоварни камиони с две температурни зони. Всяко превозно средство е оборудвано с валидирани хладилни агрегати и системи за 24/7 мониторинг.
                            </p>
                        </div>

                        <div class="w-full overflow-hidden">
                            <div class="flex gap-8 overflow-x-auto pb-8 custom-scrollbar no-scrollbar drag-scroll">
                                {% for vehicle in products %}
                                    <div class="group relative flex-shrink-0">
                                        <div class="home-product-card h-full p-2 bg-white rounded-3xl relative flex flex-col w-[260px] md:w-[300px] transition-all duration-300 shadow-md">
                                            <div class="home-product-image flex items-center justify-center overflow-hidden">
                                                <img src="{{ vehicle.images[0] | url }}" alt="{{ vehicle.title }}" class=""/>
                                            </div>

                                            <div class="pt-3 flex flex-col gap-4 flex-grow">
                                                <div class="flex flex-col gap-2 justify-between">
                                                    <h3 class="text-md font-bold text-[#25425E]">{{ vehicle.title }}</h3>
                                                    <p class="text-sm text-primary-deepsea leading-relaxed">{{ vehicle.short_description }}</p>
                                                </div>

                                                <div class="flex flex-col gap-2 text-sm text-primary-volcanic/80">
                                                    {% if vehicle.temperature_range %}
                                                        <div class="flex items-center justify-between gap-2 bg-[#25425E]/5 rounded-2xl px-3 py-2">
                                                            <span class="font-semibold text-[#25425E]">Температура</span>
                                                            <span>{{ vehicle.temperature_range }}</span>
                                                        </div>
                                                    {% endif %}
                                                    {% if vehicle.capacity %}
                                                        <div class="flex items-center justify-between gap-2 bg-[#25425E]/5 rounded-2xl px-3 py-2">
                                                            <span class="font-semibold text-[#25425E]">Капацитет</span>
                                                            <span>{{ vehicle.capacity }}</span>
                                                        </div>
                                                    {% endif %}
                                                </div>

                                                <div class="mt-2 flex flex-col flex-grow">
                                                    <a href="{{ ('/transport/' ~ vehicle.meta_title ~ '/') | url }}" class="block text-center rounded-full border border-[#25425E] text-[#25425E] font-semibold py-2 mt-auto transition-colors duration-200 hover:bg-[#25425E] hover:text-white">Виж детайли</a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </section>

                <!-- Faq -->
                <section class="py-12 ">
                    <div class="max-w-7xl w-full mx-auto px-4 flex flex-col justify-center items-center gap-12 md:gap-14">
                        <!-- Header -->
                        <div class="w-full text-left">

                            <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-primary-evergreen mb-4">
                                {{ faq.title }}
                            </h2>
                        </div>

                        <div class="faq-container grid md:grid-cols-2 gap-12 w-full items-start">
                            <div class="faq-left-side flex flex-col justify-center gap-6 order-2 md:order-1">
                                {% for item in faq.faqs %}
                                    <details class="group bg-white p-6 rounded-3xl shadow-sm" {% if loop.first %}open{% endif %}>
                                        <summary class="flex justify-between items-center cursor-pointer font-semibold text-primary-deepsea">
                                            {{ item.question }}
                                            <svg class="w-5 h-5 transition-transform duration-300 group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                                            </svg>
                                        </summary>
                                        <div class="mt-4 text-primary-dark-gray">
                                            {{ item.answer }}
                                        </div>
                                    </details>
                                {% endfor %}
                            </div>

                            <div class="faq-right-side rounded-3xl overflow-hidden order-1 md:order-2 md:sticky md:top-24">
                                <img src="{{ '/assets/images/faq.jpg' | url }}" alt="FAQ Image" class=" object-cover w-full h-auto">
                            </div>
                        </div>

                    </div>
                </section>

                <section class="py-12">
                    <div class="max-w-7xl mx-auto px-4">
                        <div class="relative h-[500px] md:h-[400px] w-full flex flex-col md:flex-row justify-evenly items-center gap-12 p-8 overflow-hidden rounded-3xl">

                            <div class="-z-10 absolute top-0 left-0 w-full h-full flex items-center justify-center">
                                <img src="{{ '/assets/images/cta2.jpg' | url }}" alt="CTA Image" class="w-full h-full object-cover ">
                            </div>

                            <div class="cta-left-side w-full sm-w-[50%] flex flex-col justify-center items-center sm:items-center text-center md:text-left">

                                <h2 class="text-white text-3xl md:text-4xl lg:text-5xl font-semibold">
                                    Време е за теб!
                                </h2>
                                <p class="text-white text-sm font-light">
                                    Подари си момент на внимание и грижа за тялото си.
                                </p>

                            </div>

                            <div class="w-full sm-w-[50%] cta-right-side ">

                                <div class="p-8 home-cta-buttons-ct cta-glassy-container flex flex-col gap-4 sm:flexcol">

                                    <p class="text-primary-volcanic text-sm font-semibold">Свържете се с нас или заявете хладилен превоз</p>
                                    <div class="flex flex-col sm:flex-row gap-4">

                                        <a href="viber://chat?number=%2B359885415300" class="text-primary-evergreen flex items-center justify-center gap-2 btn-primary w-full text-center rounded-3xl" target="_blank" rel="noopener">
                                            <img src="{{ '/assets/images/booking.png' | url }}" alt="Clock" class="w-6 h-6 object-contain "> Свържи се</a>

                                        <a href="{{ '/transport/' | url }}" class="flex items-center justify-center gap-2 btn-secondary hover:scale-105 transition-all duration-300 w-full text-center rounded-3xl  !hover:text-accent-evergreen">
                                            <img src="{{ '/assets/images/products.png' | url }}" alt="Fleet" class=" w-6 h-6 object-contain "> Автопарк</a>
                                    </div>
                                </div>

                            </div>

                        </div>
                    </div>
                </section>

                <!-- Certificates Lightbox -->
                <div
                    id="certificate-lightbox"
                    class="fixed inset-0 bg-black/80 flex items-center justify-center z-50 opacity-0 pointer-events-none transition-opacity duration-300"
                    aria-hidden="true"
                >
                    <button
                        type="button"
                        id="certificate-lightbox-close"
                        class="absolute top-4 right-4 rounded-full text-white text-3xl p-3 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary-evergreen/80"
                        aria-label="Затвори изображението"
                    >
                        ✕
                    </button>
                    <div class="max-w-[95vw] max-h-[80vh] flex items-center justify-center px-4">
                        <img
                            id="certificate-lightbox-img"
                            src=""
                            alt=""
                            class="w-full h-full max-w-[95vw] max-h-[80vh] object-contain rounded-2xl shadow-2xl border border-primary-cream/60 bg-black/10"
                        >
                    </div>
                </div>

                <script>
                    document.addEventListener('DOMContentLoaded', function () {
                        const lightbox = document.getElementById('certificate-lightbox');
                        const lightboxImg = document.getElementById('certificate-lightbox-img');
                        const closeBtn = document.getElementById('certificate-lightbox-close');

                        if (!lightbox || !lightboxImg) return;

                        const openLightbox = (src, alt) => {
                            lightboxImg.src = src;
                            lightboxImg.alt = alt || '';
                            lightbox.classList.remove('opacity-0', 'pointer-events-none');
                            lightbox.classList.add('opacity-100');
                            lightbox.setAttribute('aria-hidden', 'false');
                        };

                        const closeLightbox = () => {
                            lightbox.classList.add('opacity-0', 'pointer-events-none');
                            lightbox.classList.remove('opacity-100');
                            lightbox.setAttribute('aria-hidden', 'true');
                        };

                        document.querySelectorAll('[data-certificate-full]').forEach((el) => {
                            el.addEventListener('click', () => {
                                const src = el.getAttribute('data-certificate-full');
                                const alt = el.getAttribute('data-certificate-alt');
                                if (src) {
                                    openLightbox(src, alt);
                                }
                            });
                        });

                        if (closeBtn) {
                            closeBtn.addEventListener('click', closeLightbox);
                        }

                        lightbox.addEventListener('click', (event) => {
                            if (event.target === lightbox) {
                                closeLightbox();
                            }
                        });

                        document.addEventListener('keydown', (event) => {
                            if (event.key === 'Escape') {
                                closeLightbox();
                            }
                        });
                    });
                </script>

                <script src="{{ '/assets/js/services-grid.js' | url }}" defer></script>

                <!-- tailwind-safelist -->
                <div class="text-primary-moss text-primary-sage text-primary-ceramic text-primary-forest text-primary-cream text-primary-olive text-primary-dark-gray bg-primary-moss bg-primary-sage bg-primary-ceramic bg-primary-forest bg-primary-cream bg-primary-olive bg-gradient-primary bg-gradient-hero border-primary-moss border-primary-sage border-primary-ceramic border-primary-forest border-primary-cream border-primary-olive hidden"></div>