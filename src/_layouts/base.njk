<!DOCTYPE html>
<html lang="bg">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if metaTitle %}
                {{ metaTitle }}
            {% else %}
                {% if title %}{{ title }} | {% endif %}БиоВел Диагностика и хранителни добавки
            {% endif %}
        </title>
        <meta name="description" content="{% if metaDescription %}{{ metaDescription }}{% elif description %}{{ description }}{% else %}ВЕГА ТЕСТ - компютърна диагностика за здравословно хранене с най-подходящите храни за вашия организъм{% endif %}">

        <!-- Favicon -->
        <link rel="icon" type="image/x-icon" href="{{ '/favicon.ico' | url }}">

        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">

        <!-- Stylesheets -->
        <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}">
        <link rel="stylesheet" href="{{ '/assets/css/custom-styles.css' | url }}">
        <link rel="stylesheet" href="{{ '/assets/css/home.css' | url }}">

        <!-- Open Graph meta tags -->
        <meta property="og:title" content="{% if metaTitle %}{{ metaTitle }}{% elif title %}{{ title }}{% else %}БиоВел Диагностика и хранителни добавки{% endif %}"/>
        <meta property="og:description" content="{% if metaDescription %}{{ metaDescription }}{% elif description %}{{ description }}{% else %}ВЕГА ТЕСТ - компютърна диагностика за здравословно хранене{% endif %}"/>
        <meta property="og:type" content="website"/>

        <script src="{{ '/assets/js/gsap.min.js' | url }}"></script>
        <script src="{{ '/assets/js/ScrollTrigger.min.js' | url }}"></script>

        <!-- External Scripts -->
        <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
        <script>
            if (window.gsap && window.ScrollTrigger) {
                gsap.registerPlugin(ScrollTrigger);
            }

            // Mobile menu toggle
            document.addEventListener('DOMContentLoaded', function () {
                const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
                const mobileMenu = document.getElementById('mobile-menu');
                const body = document.body;

                if (mobileMenuToggle && mobileMenu) {
                    mobileMenuToggle.addEventListener('click', function () {
                        // Toggle burger animation
                        this
                            .classList
                            .toggle('active');

                        // Toggle mobile menu
                        if (mobileMenu.classList.contains('translate-x-full')) {
                            mobileMenu
                                .classList
                                .remove('translate-x-full');
                            mobileMenu
                                .classList
                                .add('translate-x-0');
                            body.style.overflow = 'hidden'; // Prevent body scroll
                        } else {
                            mobileMenu
                                .classList
                                .add('translate-x-full');
                            mobileMenu
                                .classList
                                .remove('translate-x-0');
                            body.style.overflow = ''; // Restore body scroll
                        }
                    });
                }

                // Mobile menu no longer uses accordion sections
            });
        </script>

        <script src="{{ '/assets/js/animations.js' | url }}"></script>

    </head>
    <body class="text-primary-volcanic font-['Montserrat'] relative">
        <!-- Background Image -->
        <div class="fixed top-0 left-0 w-full h-screen z-0" style=" opacity: 0.35;  ">
            <img src="{{ '/assets/images/body-bg.jpg' | url }}" alt="" class="w-full h-full object-cover">
        </div>

        <div class="fixed top-0 left-0 w-full h-screen z-0" style=" opacity: 0.05; background-color: #393E46; "></div>

        <!-- Header -->
        <header id="header" class="{% if page.url == '/' %}absolute top-0 left-0 right-0{% else %}relative{% endif %} z-50 flex justify-center px-4 pt-8">
            <div class="flex w-full max-w-7xl items-center gap-4 rounded-full bg-[#25425E] text-white shadow-lg border border-white/10 px-4 py-4 md:px-6">
                <a href="{{ '/' | url }}" class="flex items-center gap-3 flex-shrink-0">
                    <img src="{{ '/assets/images/logo-dark.png' | url }}" alt="Frigo Express BG" class="h-6 w-auto">
                </a>

                <nav class="hidden md:flex flex-1 justify-center">
                    <ul class="flex items-center gap-8 text-base font-medium text-white/80">
                        <li><a href="{{ '/' | url }}" class="hover:text-white transition-colors">Начало</a></li>
                        <li><a href="{{ '/za-nas/' | url }}" class="hover:text-white transition-colors">За нас</a></li>
                        <li><a href="{{ '/uslugi/' | url }}" class="hover:text-white transition-colors">Услуги</a></li>
                        <li><a href="{{ '/transport/' | url }}" class="hover:text-white transition-colors">Транспорт</a></li>
                        <li><a href="{{ '/kontakti/' | url }}" class="hover:text-white transition-colors">Контакти</a></li>
                    </ul>
                </nav>

                <div class="flex items-center gap-3 ml-auto">
                    <a href="{{ '/kontakti/' | url }}" class="hidden md:inline-flex items-center justify-center rounded-full bg-[#C8E6C9] text-[#25425E] px-6 py-2 text-sm font-medium shadow-sm hover:shadow-md transition-all duration-200">Обади ни се</a>
                    <button id="mobile-menu-toggle" class="mobile-burger flex flex-col justify-center items-center w-10 h-10 rounded-full bg-white/10 md:hidden">
                        <span class="burger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out mb-1"></span>
                        <span class="burger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out mb-1"></span>
                        <span class="burger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out"></span>
                    </button>
                </div>
            </div>
        </header>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="mobile-menu fixed inset-0 top-0 z-40 transform translate-x-full transition-transform duration-300 ease-in-out overflow-hidden bg-[#25425E]/95 backdrop-blur">
            <div class="h-full flex flex-col pt-28 pb-10 px-6 gap-8 text-white">
                <nav class="flex-1 flex flex-col space-y-4 text-xl font-medium">
                    <a href="{{ '/' | url }}" class="rounded-2xl border border-white/10 px-5 py-4 hover:bg-white/10 transition-colors">Начало</a>
                    <a href="{{ '/za-nas/' | url }}" class="rounded-2xl border border-white/10 px-5 py-4 hover:bg-white/10 transition-colors">За нас</a>
                    <a href="{{ '/uslugi/' | url }}" class="rounded-2xl border border-white/10 px-5 py-4 hover:bg-white/10 transition-colors">Услуги</a>
                    <a href="{{ '/transport/' | url }}" class="rounded-2xl border border-white/10 px-5 py-4 hover:bg-white/10 transition-colors">Транспорт</a>
                    <a href="{{ '/kontakti/' | url }}" class="rounded-2xl border border-white/10 px-5 py-4 hover:bg-white/10 transition-colors">Контакти</a>
                </nav>

                <a href="{{ '/kontakti/' | url }}" class="w-full rounded-full bg-[#C8E6C9] text-[#25425E] text-center font-medium py-3 shadow-md hover:shadow-lg transition-all duration-200">Обади ни се</a>
            </div>
        </div>

        <!-- Main Content -->
        <main class="{% if page.url == '/' %}pt-0{% else %}pt-6{% endif %} relative z-10 ">
            {{ content | safe }}
        </main>

        <!-- Footer -->
        <footer class="bg-primary-volcanic text-primary-sand mt-8 relative z-10">
            <div class="max-w-7xl mx-auto py-16 px-4">
                <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
                    <div class="col-span-1 md:col-span-2">
                        <img src="{{ '/assets/images/logo.png' | url }}" alt="Вега Тест" class="h-16 w-auto mb-4">
                        <p class="text-white/90 mb-4 max-w-md">
                            Персонализирани решения, холистични терапии и продукти, които поддържат твоето естествено равновесие.
                        </p>
                        <div class="flex items-center gap-3 text-white/80">
                            <svg class="w-6 h-6" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                                <path d="M22 12.07C22 6.48 17.52 2 11.93 2S1.86 6.48 1.86 12.07c0 4.99 3.64 9.14 8.4 9.93v-7.03H7.92v-2.9h2.34V9.97c0-2.31 1.37-3.59 3.48-3.59.99 0 2.02.18 2.02.18v2.22h-1.14c-1.12 0-1.47.7-1.47 1.42v1.7h2.5l-.4 2.9h-2.1V22c4.76-.79 8.4-4.94 8.4-9.93Z" />
                            </svg>
                            <a href="https://www.facebook.com/biovell?locale=bg_BG" target="_blank" rel="noopener" class="hover:text-white transition-colors font-semibold">Био Вел</a>
                        </div>
                    </div>

                    <div>
                        <h4 class="text-lg font-semibold mb-4">Контакти</h4>
                        <div class="space-y-2 text-white/90">
                            <p>гр. Пловдив</p>
                            <p>Хотел SPS, ет.13, офис 1302</p>
                            <p>тел: 0885 415 300</p>
                            <p>e-mail: e_hrisi@abv.bg</p>
                        </div>
                    </div>

                    <div>
                        <h4 class="text-lg font-semibold mb-4">Работно време</h4>
                        <p class="text-white/90">С предварително записан час</p>
                    </div>
                </div>

                <div class="border-t border-white/20 mt-12 pt-8 text-center text-white/70">
                    <p>&copy; {{ site.currentYear }} Bio Vel. Всички права запазени.</p>
                </div>
            </div>
        </footer>

        <script>
            // Drag-to-scroll for horizontal lists
            (function () {
                const containers = document.querySelectorAll('.drag-scroll');
                containers.forEach((el) => {
                    let isDown = false;
                    let startX = 0;
                    let scrollLeftStart = 0;

                    const onDown = (clientX) => {
                        isDown = true;
                        startX = clientX;
                        scrollLeftStart = el.scrollLeft;
                        el
                            .classList
                            .add('dragging');
                    };
                    const onMove = (clientX) => {
                        if (!isDown) 
                            return;
                        const dx = clientX - startX;
                        el.scrollLeft = scrollLeftStart - dx;
                    };
                    const onUp = () => {
                        isDown = false;
                        el
                            .classList
                            .remove('dragging');
                    };

                    // Mouse events
                    el.addEventListener('mousedown', (e) => {
                        // left button only
                        if (e.button !== 0) 
                            return;
                        onDown(e.clientX);
                        e.preventDefault();
                    });
                    window.addEventListener('mousemove', (e) => onMove(e.clientX));
                    window.addEventListener('mouseup', onUp);

                    // Touch events
                    el.addEventListener('touchstart', (e) => onDown(e.touches[0].clientX), {passive: true});
                    window.addEventListener('touchmove', (e) => onMove(e.touches[0].clientX), {passive: true});
                    window.addEventListener('touchend', onUp);
                });
            })();

             {# // Equalize description heights
             (function(){
               function equalizeDescriptions() {
                 document.querySelectorAll('.drag-scroll').forEach(row => {
                   const descriptions = row.querySelectorAll('.service-card-description');
                   if (!descriptions.length) return;
 
                   // Reset heights to get natural size
                   descriptions.forEach(desc => { desc.style.height = 'auto'; });
                   
                   // Force reflow before measuring
                   void descriptions[0].offsetHeight;
 
                   let maxHeight = 0;
                   descriptions.forEach(desc => {
                     maxHeight = Math.max(maxHeight, desc.offsetHeight);
                   });
 
                   descriptions.forEach(desc => {
                     desc.style.height = `${maxHeight}px`;
                   });
                 });
               }
               
               window.addEventListener('load', equalizeDescriptions);
               window.addEventListener('resize', equalizeDescriptions);
               const ro = new ResizeObserver(equalizeDescriptions);
               document.querySelectorAll('.drag-scroll').forEach(el => ro.observe(el));
             })(); #}
        </script>
        <script src="{{ '/assets/js/tooltips.js' | url }}"></script>
    </body>
</html>